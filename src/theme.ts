import { createTheme  } from "@mui/material";

const theme = createTheme({
    palette: {
        primary: {
            main:'#1abc9c'
        },
        secondary: {
            main: '#1abc9c'
        }
    }
})


export default theme